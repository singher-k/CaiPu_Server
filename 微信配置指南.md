# 微信小程序配置指南

## 🔑 获取微信配置信息

1. **登录微信公众平台**
   - 访问: https://mp.weixin.qq.com
   - 使用小程序管理员账号登录

2. **获取AppID和AppSecret**
   - 进入小程序管理后台
   - 点击"开发" -> "开发设置"
   - 复制 AppID 和 AppSecret

## 🖥️ 生产服务器配置

### 方法1: 设置环境变量（推荐）
```bash
# 连接到生产服务器
ssh root@120.55.4.95

# 设置环境变量
export WECHAT_APPID=你的真实AppID
export WECHAT_SECRET=你的真实AppSecret
export WECHAT_GRANT_TYPE=authorization_code

# 重启服务器使环境变量生效
pkill -f "node.*app.js"
nohup node app.js > app.log 2>&1 &
```

### 方法2: 配置.env文件
```bash
# 在服务器上编辑.env文件
nano /root/CaiPu_Server/.env

# 添加或更新以下内容：
WECHAT_APPID=你的真实AppID
WECHAT_SECRET=你的真实AppSecret
WECHAT_GRANT_TYPE=authorization_code

# 保存并重启服务器
```

## 🧪 验证配置

配置完成后，在服务器上测试：
```bash
# 检查环境变量
echo $WECHAT_APPID
echo $WECHAT_SECRET

# 测试登录API
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"code": "mock_test"}'
```

## ⚠️ 安全注意事项

1. **AppSecret是敏感信息**，请妥善保管
2. **不要将AppSecret提交到Git仓库**
3. **生产环境使用真实的微信小程序配置**
4. **测试环境可以使用测试配置**

## 🔧 常见问题

### Q: 配置后仍然返回500错误
A: 请检查：
- 环境变量是否正确设置
- 服务器是否重启
- 微信小程序是否已发布

### Q: 微信API调用失败
A: 请检查：
- AppID和AppSecret是否正确
- 服务器网络是否可以访问微信API
- 微信小程序域名配置是否正确

### Q: 如何获取真实的微信配置
A: 
1. 确保小程序已通过微信审核
2. 在微信公众平台获取正式配置
3. 更新生产服务器配置

---
**重要**: 本地测试可以使用测试配置，但生产环境必须使用真实的微信小程序配置。